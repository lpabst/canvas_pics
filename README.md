# Playing around with images on the canvas, merging images, messing around with RGB values, shifting and limiting them, etc

## to start it up
spin up the server with `node server.js` then spin up the front end with `npm start`